eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
server:
  port: 2001
spring:
  application:
    name: taxapy-taxi-server
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: roottaxappy
    url: jdbc:mysql://taxi.czwwvekl5msz.us-east-1.rds.amazonaws.com:3306/taxi?serverTimezone=UTC
    username: admin
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
